# Final feature documentation

Can you summarize the whole feature in the docs/ folder?
It should be an high level overview of the feature, with architecture, models, and if it is relevant some details about implementation.

It should point to relevant part of the codebase for future references. Please don't be too verbose, just the relevant information.

Rules:

- Find the most relevant existing doc file (docs/_.md, README.md in subdirectories, or any project-specific documentation location). Create a new docs/_.md only if no existing file fits.
- Update the doc in-place: add/modify the relevant section, don't rewrite the whole file.
- Be concise: short paragraphs, bullet points, code snippets only when necessary.
- Link to external docs (official references, RFCs, etc.) and internal code paths rather than explaining everything inline.
- Adapt to the project's existing documentation style and structure.
- If multiple files need updating (e.g. a docs/ page AND a subfolder README), update all of them.
- Don't document trivial or obvious things — focus on decisions, gotchas, and "why".
